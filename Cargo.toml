[workspace]
members = [
    "cli/pg_datanymizer",
    "datanymizer_dumper",
    "datanymizer_engine",
]

[profile.release]
opt-level = 'z'  # Optimize for size
lto = true
codegen-units = 1
panic = 'abort'
